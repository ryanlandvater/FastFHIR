*__pycache__
.venv/
.conda
.vscode/
.idea/
build*/
fhir_specs/
fhir_packages/
generated_src/
FastFHIR.xcodeproj/xcuserdata
FastFHIR.xcodeproj/project.xcworkspace/xcuserdata
*.ff

# Bazel outputs and convenience symlinks
bazel-*
bazel-build/
bazel-bin
bazel-out
bazel-testlogs
bazel-fastfhir
bazel-fastfhir.exe

# Bazel module/dependency cache trees occasionally created in-workspace
external/
execroot/
output/

.arbiter/
*.py.md

# macOS
.DS_Store

generated_src/

# Dictionary C++ — regenerated by the generator from generator/master_codes.json.
# README.md is documentation, kept committed.
dictionaries/*.cpp
dictionaries/*.hpp
!dictionaries/README.md

# NOTE: generated_src/ above also covers FF_Recovery.hpp, which moved there from
# include/ on 2026-08-19. It is generated from dictionaries/master_tags.json at
# configure time; the committed record of every tag value is that ledger plus
# tests/generator/golden/wire_witness.json. A third copy was one more thing to
# keep in step, and nothing detected a stale one.

# Rendered documentation (docs/build_docs.sh output)
docs/output/

# Session handoff notes. Internal, agent-to-agent working documents -- they
# describe what to do next, not what the project IS, and both repos are public.
# Two were tracked until 2026-09-08 (1,205 and 740 lines) and read as internal
# process noise on a repo whose value is its credibility. Write them if they
# help; do not commit them.
handoff.md
handoff*.md
HANDOFF*.md
