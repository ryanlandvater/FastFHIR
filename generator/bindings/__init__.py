# FastFHIR Generator — bindings subpackage.
# Cross-language stub emission (currently Python field stubs). Consumes the
# model and emits .py/.pyi artifacts. Like `emit/`, it depends on `model/`
# one-way and never the reverse.
